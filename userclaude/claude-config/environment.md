# Your Development Environment

<!-- Edit this file freely -- it is YOURS (seeded once, never overwritten).
     It is imported into ~/.claude/CLAUDE.md at session load. -->

- **OS / machines**: (e.g. "Windows 11 desktop -- primary dev; Linux VPS -- services; MacBook -- mobile")
- **Primary code location**: (e.g. `~/code/` -- also set `CLAUDE_CODE_ROOTS` to match, for the cross-project skills)
- **Shared tooling**: (e.g. a dotfiles repo or network drive mounted consistently across machines)
- **Off-limits for Claude**: (production paths, mounted shares, anything that must never be touched)
